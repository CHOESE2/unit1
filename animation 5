// built in variable, 
int chocolate;
int choco;
int c;

void setup() {
  size(600,600);
  chocolate = 300;
  choco = 0;
  c = 50;
}

void draw(){
  background(210);
  strokeWeight(1);
  ellipse(choco, chocolate, c, c);
choco = choco + 5;
c = c + 2;
}

  
